import project from "./project-schema";
// a medida q creamos mas schemas los metemos en el array, cuando lo vayamos a usar importar siempre este archivo solo para acceder a todos 

const schemas = [project]

export default schemas